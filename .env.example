# No secrets are needed to run scripts/run.mjs locally — feed fetching and
# rendering use only public RSS URLs.
#
# FLY_API_TOKEN is only used by the GitHub Actions deploy step; it's set as
# a GitHub Actions repo secret, never checked in here. This file exists so
# the "write new secrets locally first" habit has somewhere to go if that
# ever changes.
